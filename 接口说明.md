# B04 E2 组号与接口说明

## 小组信息

| 项目 | 内容 |
|---|---|
| 本组 | B04 |
| 配对组 | A04 |
| 产物命名空间 | pair04 |
| 本组职责 | DRAFT：生成构建环境；MDFixer：修复缺失依赖 |
| A组职责 | BuildChecker：全量检测；EChecker：增量检测 |
| 1号职责 | 公共接口整理、公共 Schema 对齐、ADR 汇总 |
| 契约版本 | schema_version 1.0.0，JSON Schema Draft 2020-12 |

本次沿用 A04 最终提交的接口文件，来源源码版本为 `407bec149bbe4326b61fa73db86252252a652491`。Schema 保持原样，不将 A04 的实现记为 B04 新写的代码。E2 只定义和验证契约，不要求部署 API。

## 接口流程

创建请求携带 `Idempotency-Key` 请求头，长度为8–128字符。同键同请求返回相同 job_id。创建成功返回 HTTP 202、服务端生成的 job_id 和 QUEUED，随后查询任务状态与结果。

| 操作 | 方法与路径 | job_type |
|---|---|---|
| 生成构建环境 | POST /v1/dockerfile-jobs | DRAFT |
| 全量检测 | POST /v1/full-check-jobs | FULL_CHECK |
| 增量检测 | POST /v1/incremental-check-jobs | INCREMENTAL_CHECK |
| 修复缺失依赖 | POST /v1/repair-jobs | REPAIR |
| 查询任务 | GET /v1/jobs/{job_id} | 全部 |

## 公共 Schema

以下文件均在本文同目录的 contracts/v1 文件夹中。

| 文件 | 用途 |
|---|---|
| [create-request.schema.json](contracts/v1/create-request.schema.json) | 四类创建请求、公共字段及服务最低输入要求 |
| [create-response.schema.json](contracts/v1/create-response.schema.json) | HTTP 202 受理响应 |
| [job.schema.json](contracts/v1/job.schema.json) | 查询结果、状态、错误和服务输出 |
| [artifact.schema.json](contracts/v1/artifact.schema.json) | 产物引用与完整性字段 |

创建正文：schema_version、trace_id、job_type、execution、input。

受理响应：schema_version、job_id、trace_id、job_type、status、links.self。

查询响应：schema_version、job_id、trace_id、job_type、status、input、output、error、links.self。查询响应不含 execution。

## 状态和错误

状态统一为 QUEUED、RUNNING、SUCCEEDED、FAILED、TIMED_OUT、CANCELLED。

- SUCCEEDED：output 为结果对象，error 为 null；检测发现 MD/RD 仍可成功结束。
- FAILED、TIMED_OUT：output 为 null，error 为系统错误对象。
- QUEUED、RUNNING、CANCELLED：output 和 error 均为 null。

系统错误包含 code、message、retryable，details 可选。错误码沿用 CONTRACT_1001、BASELINE_2001、ENV_3002、EXEC_4002、ANALYSIS_5001。MD/RD 放入报告的 findings，不放进 job.error。

无效创建请求使用 HTTP 400；查询已有任务使用 HTTP 200，任务不存在使用 HTTP 404。400/404 的完整响应正文、同键不同请求和幂等有效期在 A04 当前材料中尚未完整定义，需双方补充后使用。

## B组输入输出与产物交接

DRAFT 使用 repository、project_root、build、max_iterations。build 包含 command 和 verify_command；结果包含 dockerfile、image、iterations、final。

REPAIR 使用 repository、error_report_uri、target_file、environment、build。环境字段为 image_uri 和 configuration_id；结果包含 patch、repair_report、validation。

REPAIR 只接受 MISSING。报告顶层 repository_commit 必须等于输入 repository.commit，configuration_id 必须与运行环境一致。commit 使用40位小写十六进制完整 SHA。

文件使用 artifact://pair04/...，由本包沿用的 validate.py 映射到 artifacts/pair04/...。本包已收录对应 artifacts、examples、validate.py 和依赖文件，可按提交说明运行校验。

产物引用必填 artifact_id、type、uri、media_type、producer_job_id、sha256。文件读取后核对 SHA-256；文本换行变化也会影响哈希。Patch 类型使用 GIT_PATCH。

## ADR 汇总

公共决策见 [ADR-001](adr/ADR-001-公共任务与版本.md) 和 [ADR-002](adr/ADR-002-产物交接与服务决策.md)。1号维护公共部分，2号补充 DRAFT 决策，3号补充 MDFixer 决策。尚未讨论的服务方案标为待补，不记为已确认。
